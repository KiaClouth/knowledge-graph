# ── 复制本文件为 .env 并填写真实值。切勿提交 .env。 ──

# 【必填】对话(LLM)用的 key
OPENAI_API_KEY=sk-...

# 【可选】自定义 OpenAI 兼容端点。指向中转/代理/Azure/本地服务时填写。
#         留空则用 OpenAI 官方默认地址。
# OPENAI_BASE_URL=https://your-endpoint/v1

# 【可选】抽取用的对话模型(默认 gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# 【可选】Graphiti 把"简单"任务(属性抽取/去重)路由到 small_model。
#         留空则用上面的主模型(安全)。⚠️ 切勿留给 Graphiti 默认值
#         gpt-4.1-nano —— 很多中转端点不提供该模型,会 503 崩。
#         仅当你的端点确有更便宜的小模型时才显式指定。
# OPENAI_SMALL_MODEL=

# ── 嵌入(embeddings)单独配置 ──
# Graphiti 同时需要"对话"和"嵌入"两种能力。很多中转端点只提供对话、不提供
# 嵌入。如果你的对话端点没有嵌入能力,就在这里把嵌入指向另一个有嵌入的端点
# (例如 OpenAI 官方)。下面三项留空时,默认复用上面的 KEY / BASE_URL。
# EMBEDDER_API_KEY=sk-...
# EMBEDDER_BASE_URL=https://api.openai.com/v1
# EMBEDDER_MODEL=text-embedding-3-small
# 嵌入向量维度 —— 必须与端点实际输出一致,且首次写入后固化进 Kùzu schema,
# 之后换模型需重建库。OpenAI text-embedding-3-small=1536;智谱 embedding-3=2048。
# EMBEDDER_DIM=1536
